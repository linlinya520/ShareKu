<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <uses-feature android:name="android.hardware.wifi" android:required="false" />

    <application
        android:name=".ShareKuApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MaterialShareKu"
        tools:targetApi="35">

        <!-- Main Launcher Activity -->
        <activity
            android:name=".MainActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MaterialShareKu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Share Handling Activity - receives files from other apps -->
        <activity
            android:name=".ShareActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.MaterialShareKu.ShareSheet">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <action android:name="android.intent.action.SEND_MULTIPLE" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
        </activity>

        <!-- Bubble Activity - shown in Android Bubble -->
        <activity
            android:name=".BubbleActivity"
            android:exported="false"
            android:allowEmbedded="true"
            android:taskAffinity=".bubble"
            android:theme="@style/Theme.MaterialShareKu.Bubble"
            android:documentLaunchMode="always"
            android:resizeableActivity="true"
            tools:targetApi="31" />

        <!-- Settings Activity -->
        <activity
            android:name=".SettingsActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="false"
            android:label="@string/settings"
            android:theme="@style/Theme.MaterialShareKu" />

        <!-- Settings: Security -->
        <activity
            android:name=".ui.screen.SecurityActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="false"
            android:theme="@style/Theme.MaterialShareKu" />

        <!-- Settings: Server -->
        <activity
            android:name=".ui.screen.ServerActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="false"
            android:theme="@style/Theme.MaterialShareKu" />

        <!-- Settings: FileOps -->
        <activity
            android:name=".ui.screen.FileOpsActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="false"
            android:theme="@style/Theme.MaterialShareKu" />

        <!-- Settings: CacheCleanup -->
        <activity
            android:name=".ui.screen.CacheCleanupActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="false"
            android:theme="@style/Theme.MaterialShareKu" />

        <!-- Settings: Appearance -->
        <activity
            android:name=".ui.screen.AppearanceActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="false"
            android:theme="@style/Theme.MaterialShareKu" />

        <!-- Settings: About -->
        <activity
            android:name=".ui.screen.AboutActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="false"
            android:theme="@style/Theme.MaterialShareKu" />

        <!-- Settings: Changelog -->
        <activity
            android:name=".ui.screen.ChangelogActivity"
            android:enableOnBackInvokedCallback="true"
            android:exported="false"
            android:theme="@style/Theme.MaterialShareKu" />

        <!-- Foreground Service for HTTP server -->
        <service
            android:name=".service.ServerForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- Boot receiver for auto-start -->
        <receiver
            android:name=".receiver.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- File Provider for sharing URIs -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>